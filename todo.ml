*change the names of the emotions
*uncomment and clean up code in music.ml
*put all of the decoded songs in their respective SINGLE song files
*actually code the read function (already pseudocoded)
*write probability function 
    with random int generator
    pick the new note
    add it to the stream (side effect)
    output a vector (next note and state)



